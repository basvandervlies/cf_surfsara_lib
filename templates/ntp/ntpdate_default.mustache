###
# Managed by: CFengine

{{#classes.debian}}
NTPSERVERS="{{#vars.scl.ntp.server}} {{{.}}} {{/vars.scl.ntp.server}}"
#
# additional options for ntpdate
#NTPOPTIONS="-v"
#
NTPOPTIONS="-u"
{{/classes.debian}}

{{#classes.centos}}
# Options for ntpdate
OPTIONS="-U ntp -s -b {{#vars.scl.ntp.server}} {{{.}}} {{/vars.scl.ntp.server}}"

# Set to 'yes' to sync hw clock after successful ntpdate
SYNC_HWCLOCK=no
{{/classes.centos}}

{{#classes.SLES}}
# SUSE does not have a seperate ntpdate sysconfig file
{{/classes.SLES}}
