#!/bin/sh

### File is generated by CFENGINE


### THis is standard environment
#
PATH=$PATH:/opt/configurable-http-proxy/current/bin
JUPYTERHUB_DIR={{{hub_dir}}}/{{{hub_version}}}
CONFIG_FILE={{{hub_dir}}}/etc/{{{name}}}.py

## pipenv is not needed to start
#cd ${JUPYTERHUB_DIR}
#exec /usr/local/bin/pipenv run jupyterhub --config ${CONFIG_FILE}

### Start the jupytherhub
#
source ${JUPYTERHUB_DIR}/.venv/bin/activate
exec jupyterhub --config ${CONFIG_FILE}

