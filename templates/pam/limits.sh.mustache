#!/bin/sh -e

PAM_UID=$(getent passwd "${PAM_USER}" | cut -d: -f3)

if [ "${PAM_UID}" -ge 1000 ]; then
    /bin/systemctl set-property "user-${PAM_UID}.slice" CPUQuota=400% CPUAccounting=true MemoryMax=16G MemoryAccounting=true TasksMax=10000 TasksAccounting=true
fi
